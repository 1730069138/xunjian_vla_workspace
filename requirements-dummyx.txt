# requirements-dummyx.txt
# 版本全部对齐 ~/VLA/openpi/.venv (uv, Python 3.11) 的 uv pip freeze
# 用法：
#   conda create -n dummyx python=3.11 -y && conda activate dummyx
#   conda install -y -c conda-forge "ffmpeg<8" "av=14.4.0"
#   pip install -r requirements-dummyx.txt
#   pip install --no-deps -e ~/lerobot          # git checkout 0cf864870cf29f4738d3ade893e6fd13fbd7cdb5
#   pip install -e ~/VLA/openpi/packages/openpi-client

# ---------- 三个脚本直接 import 的 ----------
numpy==1.26.4
opencv-python==4.11.0.86
mujoco==2.3.7
tqdm==4.67.1
tyro==0.9.22

# ---------- lerobot 运行期依赖（决定数据集落盘格式，必须逐一对齐） ----------
torch==2.7.1
torchvision==0.22.1
torchcodec==0.4.0
av==14.4.0
datasets==3.6.0
huggingface-hub==0.32.3
hf-transfer==0.1.9
hf-xet==1.1.2
pyarrow==20.0.0
pandas==2.2.3
pillow==11.2.1
safetensors==0.5.3
transformers==4.53.2
tokenizers==0.21.1
imageio==2.37.0
imageio-ffmpeg==0.6.0
diffusers==0.33.1
einops==0.8.1
gymnasium==0.29.1
jsonlines==4.0.0
numba==0.61.2
llvmlite==0.44.0
omegaconf==2.3.0
draccus==0.10.0
deepdiff==8.5.0
zarr==3.0.8
numcodecs==0.16.1
rerun-sdk==0.23.1
termcolor==3.1.0
wandb==0.19.11
packaging==25.0
scipy==1.15.3
jinja2==3.1.6
filelock==3.18.0
pynput==1.8.1
evdev==1.9.2
flask==3.1.1

# ---------- deploy_screwdriver_client2.py 的 openpi_client ----------
websockets==15.0.1
msgpack==1.1.0

# 注意：不要装 opencv-python-headless。旧环境里两个 opencv 同时存在是
# lerobot 拉进来的副作用，只保留 opencv-python 才能正常开 MuJoCo viewer 窗口。