# 项目代码修改规则

本文件适用于本项目后续的所有代码与配置修改。

## 修改前确认

1. 收到修改需求后，先向用户复述以下内容：
   - 修改目标
   - 修改范围
   - 预期功能或行为
   - 验收或验证方式
2. 如需求存在歧义，应先提出问题并确认，不得自行选择可能明显改变结果的方案。
3. 只有在用户明确回复“确认修改”或表达同等明确授权后，才能修改代码、配置或项目文件。
4. 用户确认后，只能在已确认的范围内修改；如需扩大范围，应再次说明并取得确认。

## 修改记录

1. 每次实际修改代码、配置或项目文件时，都必须同步更新项目根目录的 `CHANGELOG.md`。
2. 每条记录至少包含：
   - 修改时间（含时区）
   - 修改文件
   - 修改点
   - 新增、修复或调整的功能
   - 验证方式与结果
3. 若修改尚未验证，应明确标注“未验证”及原因，不得写成验证通过。
4. 同一项已确认需求涉及多个文件时，可以合并为一条记录，但必须列出所有相关文件。

## 执行边界

1. 未经确认，不执行顺带重构、依赖升级、格式化整个项目或其他范围外改动。
2. 紧急修复、小改动和文档调整也遵循上述确认与记录流程。
3. 只读检查、需求分析和方案说明不视为代码修改，可以在确认前进行，但不得改变项目文件。

